<nav class="navbar navbar-expand-lg mb-4">
    <a class="navbar-brand" href="#">
        LOGO
    </a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav ml-auto">
            {% if app.user %}
            <a class="nav-item nav-link active" href="{{path('app_home')}}">Feed <span class="sr-only">(current)</span></a>
            <a class="nav-item nav-link" href="{{path('app_category')}}">Catégories</a>
            <a class="nav-item nav-link disabled" href="#">Amis</a>
            <a class="nav-item nav-link" href="#">Mon compte</a>
            {% endif %}
        </div>
    </div>
</nav>